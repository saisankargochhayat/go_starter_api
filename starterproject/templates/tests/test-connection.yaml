apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "starterproject.fullname" . }}-test-connection"
  labels:
    {{- include "starterproject.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "starterproject.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
