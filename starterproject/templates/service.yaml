apiVersion: v1
kind: Service
metadata:
  name: {{ include "starterproject.fullname" . }}
  labels:
    {{- include "starterproject.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "starterproject.selectorLabels" . | nindent 4 }}
